# 🚀 Quick Setup Guide cho Team Members

## ⚡ TL;DR - Chạy ngay

```bash
# 1. Clone repo
git clone https://github.com/khanhhtapcode/FinTrack-App.git
cd FinTrack-App

# 2. Get dependencies
flutter pub get

# 3. 🔥 QUAN TRỌNG - Generate code
flutter pub run build_runner build --delete-conflicting-outputs

# 4. Run app
flutter run
```

---

## ❗ Vấn đề thường gặp khi Pull

### 1️⃣ **File `.g.dart` bị thiếu**
```
❌ Error: Cannot find 'UserAdapter'
❌ Error: Cannot find 'TransactionAdapter'
```

**Giải pháp:**
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

**Giải thích:**
- File `.g.dart` được generate bởi `build_runner`
- **KHÔNG được push lên Git** (có trong `.gitignore`)
- Mỗi người phải generate lại trên máy của mình

---

### 2️⃣ **Plugins chưa được build**
```
❌ MissingPluginException
```

**Giải pháp:**
```bash
flutter clean
flutter pub get
flutter run
```

---

### 3️⃣ **Conflict khi pull**
```
❌ CONFLICT (content): Merge conflict in lib/models/user.g.dart
```

**Giải pháp:**
```bash
# Xóa file conflict
git checkout --theirs lib/models/*.g.dart

# Hoặc xóa hết và generate lại
flutter pub run build_runner clean
flutter pub run build_runner build --delete-conflicting-outputs
```

---

## 📋 Checklist đầy đủ

### Lần đầu setup:
- [ ] Cài Flutter SDK >= 3.8.1
- [ ] Clone repository
- [ ] `flutter pub get`
- [ ] **Generate code: `build_runner build`** ⚠️
- [ ] Check `google-services.json` có trong `android/app/`
- [ ] Run app và test login

### Mỗi lần pull code mới:
- [ ] `git pull origin main`
- [ ] `flutter pub get` (nếu có thay đổi dependencies)
- [ ] `flutter pub run build_runner build --delete-conflicting-outputs` (nếu models thay đổi)
- [ ] `flutter clean` (nếu có lỗi lạ)
- [ ] `flutter run`

---

## 🔧 Build Runner - Chi tiết

### Khi nào cần chạy build_runner?

✅ **CẦN chạy:**
- Lần đầu clone project
- Sau khi pull code có thay đổi models
- Khi thêm/sửa `@HiveType` trong models
- Khi gặp lỗi TypeAdapter

❌ **KHÔNG cần chạy:**
- Thay đổi UI
- Thay đổi logic trong screens/services
- Thêm dependencies không liên quan đến code generation

### Commands:

```bash
# Build một lần
flutter pub run build_runner build

# Build và xóa file cũ (recommended)
flutter pub run build_runner build --delete-conflicting-outputs

# Watch mode (auto-generate khi có thay đổi)
flutter pub run build_runner watch

# Clean generated files
flutter pub run build_runner clean
```

---

## 📁 Files KHÔNG nên push

Đã được thêm vào `.gitignore`:
- `**/*.g.dart` - Generated by build_runner
- `**/*.freezed.dart` - Generated by freezed (nếu dùng)
- `.flutter-plugins`
- `.flutter-plugins-dependencies`
- `build/`
- `.dart_tool/`

---

## 🎯 Test App

### 1. Test Login thường:
```
Đăng ký tài khoản mới
→ Nhập OTP (check email)
→ Chọn preferences
→ Vào Home Screen
```

### 2. Test Admin:
```
Email: admin@fintracker.com
Password: Admin@123
→ Vào Admin Panel
→ Check Database Management
```

### 3. Test OCR:
```
Home → Thêm giao dịch (+)
→ Click icon camera
→ Chọn "Quét từ Camera" hoặc "Chọn từ thư viện"
→ Chụp/chọn ảnh hóa đơn
→ App tự nhận số tiền
```

---

## 🆘 SOS - Khi mọi thứ không hoạt động

### Nuclear option (Clean everything):
```bash
# 1. Clean Flutter
flutter clean

# 2. Clean build_runner
flutter pub run build_runner clean

# 3. Get dependencies lại
flutter pub get

# 4. Generate code lại
flutter pub run build_runner build --delete-conflicting-outputs

# 5. Restart IDE
# Đóng và mở lại Android Studio/VS Code

# 6. Run
flutter run
```

### Vẫn lỗi? Check:
- [ ] Flutter version >= 3.8.1: `flutter --version`
- [ ] Dart version >= 3.8.1: `dart --version`
- [ ] Có file `google-services.json` trong `android/app/`
- [ ] Đã enable Developer mode trên điện thoại
- [ ] Emulator đang chạy: `flutter devices`

---

## 📖 Full Documentation

- **README.md** - Hướng dẫn đầy đủ
- **ADMIN_GUIDE.md** - Admin Panel guide
- **TRANSACTION_STORAGE_GUIDE.md** - Chi tiết về storage
- **BACKEND_SETUP.md** - Firebase setup

---

**Vẫn gặp vấn đề?** → Liên hệ KHANH hoặc tạo Issue trên GitHub

---

Last updated: 16/11/2025
