class AppConstants {
  // App Info (GIỮ NGUYÊN)
  static const String appName = 'Money Flow';
  static const String appSlogan = 'Smart spend, Bright future';

  // Spacing (GIỮ NGUYÊN)
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;

  // Animation durations (GIỮ NGUYÊN)
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration animationDuration = Duration(milliseconds: 1500);

  // ===== THÊM MỚI - Không ảnh hưởng code cũ =====

  // Thêm spacing cho các màn hình mới
  static const double paddingTiny = 4.0;
  static const double paddingXLarge = 32.0;

  // Border Radius cho Home, Transaction screens
  static const double radiusSmall = 8.0;
  static const double radiusMedium = 12.0;
  static const double radiusLarge = 16.0;

  // Icon Sizes
  static const double iconSmall = 20.0;
  static const double iconMedium = 24.0;
  static const double iconLarge = 32.0;

  // Animation ngắn cho button press, dialog
  static const Duration shortAnimation = Duration(milliseconds: 300);

  // Currency formatting
  static const String currencySymbol = 'VND';
  static const String dateFormat = 'dd/MM/yyyy';
}
